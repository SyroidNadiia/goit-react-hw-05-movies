const Movies = () => {
  return (
    <main>
      <form>
        <input
          //   value={query}
          //   onChange={handleInputChange}
          type="text"
          autoComplete="off"
          autoFocus
        />
        <button type="submit">Search</button>
      </form>
    </main>
  );
};

export default Movies;
